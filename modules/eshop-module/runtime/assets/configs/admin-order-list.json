{
  "syscode": "admin-order-list",
  "restUrl": "/api/admin/order",
  "factory": {
    "url": "${name}--$${_id}"
  },
  "fields": [
    {
      "name": "_id",
      "label": "$.order.order_id"
    },
    {
      "name": "user.email",
      "label": "$.order.user_email"
    },
    {
      "name": "user.phone",
      "label": "$.order.user_phone"
    },
    {
      "name": "delivery.type",
      "label": "$.order.delivery_type"
    },
    {
      "name": "payment.type",
      "label": "$.order.payment_type"
    },
    {
      "name": "totalPrice",
      "type": "number",
      "label": "$.order.total_price"
    }
  ]
}

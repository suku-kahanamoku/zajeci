{
  "syscode": "admin-invoice-update",
  "restUrl": "/api/admin/invoice/${route.meta.id}",
  "patchUrl": "/api/admin/invoice/${route.meta.id}/status",
  "method": "PATCH",
  "fields": [
    {
      "name": "status",
      "type": "select",
      "label": "$.invoice.status",
      "required": true,
      "options": [
        { "label": "$.status.pending", "value": "pending" },
        { "label": "$.status.paid", "value": "paid" },
        { "label": "$.status.cancelled", "value": "cancelled" },
        { "label": "$.status.overdue", "value": "overdue" }
      ]
    }
  ]
}
